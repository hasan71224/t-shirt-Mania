import React from 'react';

const Uncle = ({house}) => {
    return (
        <div>
            <h1>this is uncle page</h1>
            <p>House: {house}</p>
        </div>
    );
};

export default Uncle;